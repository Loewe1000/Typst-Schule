[package]
name = "typlace-optimierer"
version = "0.0.2"
edition = "2021"
description = "Sitzordnungs-Optimierer als WASM-Plugin für das Typst-Paket typlace"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
