[package]
name = "arbeitsblatt"
version = "0.1.7"
entrypoint = "arbeitsblatt.typ"
exclude = ["/assets/*", "*.pdf", "example.typ"]
compiler = "0.12"

description = "Package for arbeitsblatt"
license = "MIT"
